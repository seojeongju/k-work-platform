# Production Environment Variables Template
# ⚠️  이 파일은 템플릿입니다. 실제 production 환경에서는 강력한 값으로 변경하세요!

# Cloudflare 설정 (배포 시 Wrangler Secrets로 관리)
CLOUDFLARE_API_TOKEN=your-production-cloudflare-token

# 보안 키 (강력한 랜덤 값 사용)
JWT_SECRET=your-super-strong-production-jwt-secret-256-bits-minimum

# 도메인 설정
DOMAIN_NAME=w-campus.com
SITE_NAME=WOW-CAMPUS
ENVIRONMENT=production

# 데이터베이스 (Wrangler.toml에서 관리)
DATABASE_NAME=w-campus-production

# 보안 설정
ENABLE_RATE_LIMITING=true
RATE_LIMIT_REQUESTS=20
RATE_LIMIT_WINDOW=300000

# CORS 설정
ALLOWED_ORIGINS=https://w-campus.com,https://www.w-campus.com

# 로깅 및 모니터링
LOG_LEVEL=warn
ENABLE_SECURITY_LOGGING=true

# 쿠키 보안
COOKIE_SECURE=true
COOKIE_SAMESITE=strict
COOKIE_HTTPONLY=true

# 세션 관리
SESSION_TIMEOUT=86400000
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION=1800000

# Note: 실제 배포시 아래 명령으로 시크릿 설정
# wrangler secret put CLOUDFLARE_API_TOKEN
# wrangler secret put JWT_SECRET